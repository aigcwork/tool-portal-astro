---
export interface Props {
  className?: string;
  showSocialLinks?: boolean;
  showContactInfo?: boolean;
}

const { 
  className = '',
  showSocialLinks = true,
  showContactInfo = true
} = Astro.props;

// Get current year
const currentYear = new Date().getFullYear();

// Footer navigation links
const footerLinks = {
  product: [
    { name: '所有工具', href: '/', target: '_self' },
    { name: '分类浏览', href: '/categories', target: '_self' },
    { name: '新增工具', href: '/', target: '_self' },
    { name: '推荐工具', href: '/', target: '_self' }
  ],
  company: [
    { name: '关于我们', href: '/about', target: '_self' },
    { name: '联系我们', href: '#contact', target: '_self' },
    { name: '用户反馈', href: 'mailto:feedback@toolportal.com', target: '_blank' },
    { name: '合作伙伴', href: '#partners', target: '_self' }
  ],
  legal: [
    { name: '隐私政策', href: '/privacy', target: '_self' },
    { name: '服务条款', href: '/terms', target: '_self' },
    { name: '免责声明', href: '/disclaimer', target: '_self' },
    { name: 'Cookie政策', href: '/cookies', target: '_self' }
  ]
};

// Social media links
const socialLinks = [
  {
    name: 'GitHub',
    href: 'https://github.com/toolportal',
    icon: 'M12 2C6.477 2 2 6.477 2 12c0 4.42 2.865 8.17 6.839 9.49.5.092.682-.217.682-.482 0-.237-.009-.868-.014-1.703-2.782.603-3.369-1.34-3.369-1.34-.454-1.156-1.11-1.463-1.11-1.463-.908-.62.069-.608.069-.608 1.003.07 1.531 1.03 1.531 1.03.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.11-4.555-4.943 0-1.091.39-1.984 1.029-2.683-.103-.253-.446-1.27.098-2.647 0 0 .84-.269 2.75 1.025A9.578 9.578 0 0112 6.836c.85.004 1.705.114 2.504.336 1.909-1.294 2.747-1.025 2.747-1.025.546 1.377.203 2.394.1 2.647.64.699 1.028 1.592 1.028 2.683 0 3.842-2.339 4.687-4.566 4.935.359.309.678.919.678 1.852 0 1.336-.012 2.415-.012 2.743 0 .267.18.578.688.48A10.001 10.001 0 0022 12c0-5.523-4.477-10-10-10z',
    color: '#333'
  },
  {
    name: 'Twitter',
    href: 'https://twitter.com/toolportal',
    icon: 'M22.46 6c-.77.35-1.6.58-2.46.69.88-.53 1.56-1.37 1.88-2.38-.83.5-1.75.85-2.72 1.05C18.37 4.5 17.26 4 16 4c-2.35 0-4.27 1.92-4.27 4.29 0 .34.04.67.11.98C8.28 9.09 5.11 7.38 3 4.79c-.37.63-.58 1.37-.58 2.15 0 1.49.75 2.81 1.91 3.56-.71 0-1.37-.2-1.95-.5v.03c0 2.08 1.48 3.82 3.44 4.21a4.22 4.22 0 0 1-1.93.07 4.28 4.28 0 0 0 4 2.98 8.521 8.521 0 0 1-5.33 1.84c-.34 0-.68-.02-1.02-.06C3.44 20.29 5.7 21 8.12 21 16 21 20.33 14.46 22 8.79c0-.19 0-.37-.01-.56.84-.6 1.56-1.36 2.14-2.23z',
    color: '#1DA1F2'
  },
  {
    name: 'LinkedIn',
    href: 'https://linkedin.com/company/toolportal',
    icon: 'M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z',
    color: '#0077B5'
  },
  {
    name: 'Email',
    href: 'mailto:contact@toolportal.com',
    icon: 'M22 4H2c-.55 0-1 .45-1 1v14c0 .55.45 1 1 1h20c.55 0 1-.45 1-1V5c0-.55-.45-1-1-1zm-2 4l-8 5-8-5V6l8 5 8-5v2z',
    color: '#EA4335'
  }
];

// Contact information
const contactInfo = {
  email: 'contact@toolportal.com',
  phone: '+86 400-123-4567',
  address: '北京市朝阳区科技园区创新大厦8层',
  businessHours: '周一至周五 9:00-18:00'
};
---

<footer class={`site-footer ${className}`} role="contentinfo">
  <div class="site-footer__container">
    <!-- Main Footer Content -->
    <div class="site-footer__main">
      <!-- Brand Section -->
      <div class="site-footer__section site-footer__brand">
        <div class="site-footer__brand-content">
          <div class="site-footer__logo">
            <span class="site-footer__logo-icon">🛠️</span>
            <span class="site-footer__logo-text">工具门户</span>
          </div>
          <p class="site-footer__description">
            发现和使用各种实用工具，提升您的工作效率。我们致力于为用户提供最优质的在线工具资源。
          </p>
          {showSocialLinks && (
            <div class="site-footer__social">
              <h3 class="site-footer__social-title">关注我们</h3>
              <ul class="site-footer__social-list" role="list">
                {socialLinks.map((social) => (
                  <li class="site-footer__social-item">
                    <a
                      href={social.href}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="site-footer__social-link"
                      aria-label={`关注我们的${social.name}`}
                      title={social.name}
                    >
                      <svg
                        class="site-footer__social-icon"
                        viewBox="0 0 24 24"
                        fill="currentColor"
                        role="img"
                        aria-hidden="true"
                      >
                        <path d={social.icon} />
                      </svg>
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          )}
        </div>
      </div>

      <!-- Navigation Links -->
      <div class="site-footer__section site-footer__nav-section">
        <h3 class="site-footer__section-title">产品服务</h3>
        <ul class="site-footer__nav-list" role="list">
          {footerLinks.product.map((link) => (
            <li class="site-footer__nav-item">
              <a
                href={link.href}
                target={link.target}
                rel={link.target === '_blank' ? 'noopener noreferrer' : undefined}
                class="site-footer__nav-link"
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Company Links -->
      <div class="site-footer__section site-footer__nav-section">
        <h3 class="site-footer__section-title">公司信息</h3>
        <ul class="site-footer__nav-list" role="list">
          {footerLinks.company.map((link) => (
            <li class="site-footer__nav-item">
              <a
                href={link.href}
                target={link.target}
                rel={link.target === '_blank' ? 'noopener noreferrer' : undefined}
                class="site-footer__nav-link"
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Legal Links & Contact -->
      <div class="site-footer__section site-footer__nav-section">
        <h3 class="site-footer__section-title">法律条款</h3>
        <ul class="site-footer__nav-list" role="list">
          {footerLinks.legal.map((link) => (
            <li class="site-footer__nav-item">
              <a
                href={link.href}
                target={link.target}
                rel={link.target === '_blank' ? 'noopener noreferrer' : undefined}
                class="site-footer__nav-link"
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
        
        {showContactInfo && (
          <div class="site-footer__contact">
            <h3 class="site-footer__contact-title">联系方式</h3>
            <div class="site-footer__contact-info">
              <a href={`mailto:${contactInfo.email}`} class="site-footer__contact-item">
                <svg class="site-footer__contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                  <polyline points="22,6 12,13 2,6"/>
                </svg>
                <span>{contactInfo.email}</span>
              </a>
              <a href={`tel:${contactInfo.phone.replace(/\s/g, '')}`} class="site-footer__contact-item">
                <svg class="site-footer__contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                </svg>
                <span>{contactInfo.phone}</span>
              </a>
              <div class="site-footer__contact-item">
                <svg class="site-footer__contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
                  <circle cx="12" cy="10" r="3"/>
                </svg>
                <span>{contactInfo.address}</span>
              </div>
            </div>
          </div>
        )}
      </div>
    </div>

    <!-- Bottom Footer -->
    <div class="site-footer__bottom">
      <div class="site-footer__bottom-content">
        <!-- Copyright -->
        <div class="site-footer__copyright">
          <p class="site-footer__copyright-text">
            © {currentYear} 工具门户网站. 保留所有权利.
          </p>
          <p class="site-footer__copyright-subtext">
            京ICP备12345678号-1 | 京公网安备11010502012345号
          </p>
        </div>

        <!-- Additional Links -->
        <div class="site-footer__additional-links">
          <a href="/sitemap" class="site-footer__additional-link">网站地图</a>
          <a href="/accessibility" class="site-footer__additional-link">无障碍声明</a>
          <a href="/help" class="site-footer__additional-link">帮助中心</a>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--bg-secondary);
    border-top: 1px solid var(--border-primary);
    margin-top: auto;
    position: relative;
  }

  .site-footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--color-primary), transparent);
    opacity: 0.3;
  }

  .site-footer__container {
    max-width: var(--container-xl);
    margin: 0 auto;
    padding: 0 var(--space-lg);
  }

  .site-footer__main {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: var(--space-2xl);
    padding: var(--space-3xl) 0 var(--space-2xl);
  }

  .site-footer__section {
    min-width: 0;
  }

  .site-footer__brand {
    display: flex;
    flex-direction: column;
  }

  .site-footer__brand-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .site-footer__logo {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    text-decoration: none;
    color: var(--text-primary);
    font-weight: 600;
    font-size: 1.25rem;
    margin-bottom: var(--space-md);
  }

  .site-footer__logo-icon {
    font-size: 1.75rem;
    filter: drop-shadow(0 1px 2px rgba(0, 0, 0, 0.1));
  }

  .site-footer__logo-text {
    user-select: none;
  }

  .site-footer__description {
    color: var(--text-secondary);
    font-size: 0.875rem;
    line-height: 1.6;
    margin: 0;
  }

  .site-footer__social {
    margin-top: var(--space-lg);
  }

  .site-footer__social-title {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0 0 var(--space-sm) 0;
  }

  .site-footer__social-list {
    display: flex;
    gap: var(--space-sm);
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .site-footer__social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: var(--radius-md);
    background: var(--bg-card);
    border: 1px solid var(--border-primary);
    color: var(--text-secondary);
    text-decoration: none;
    transition: all var(--transition-fast);
    position: relative;
    overflow: hidden;
  }

  .site-footer__social-link:hover {
    color: var(--color-primary);
    border-color: var(--color-primary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .site-footer__social-icon {
    width: 16px;
    height: 16px;
  }

  .site-footer__section-title {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0 0 var(--space-md) 0;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .site-footer__nav-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .site-footer__nav-link {
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.875rem;
    transition: color var(--transition-fast);
    position: relative;
  }

  .site-footer__nav-link:hover {
    color: var(--color-primary);
  }

  .site-footer__nav-link::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--color-primary);
    transition: width var(--transition-fast);
  }

  .site-footer__nav-link:hover::after {
    width: 100%;
  }

  .site-footer__contact {
    margin-top: var(--space-lg);
  }

  .site-footer__contact-title {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0 0 var(--space-sm) 0;
  }

  .site-footer__contact-info {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .site-footer__contact-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-sm);
    color: var(--text-secondary);
    font-size: 0.75rem;
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .site-footer__contact-item:hover {
    color: var(--color-primary);
  }

  .site-footer__contact-icon {
    width: 14px;
    height: 14px;
    margin-top: 2px;
    flex-shrink: 0;
  }

  .site-footer__bottom {
    border-top: 1px solid var(--border-primary);
    padding: var(--space-lg) 0;
  }

  .site-footer__bottom-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-lg);
  }

  .site-footer__copyright {
    text-align: left;
  }

  .site-footer__copyright-text {
    color: var(--text-secondary);
    font-size: 0.875rem;
    margin: 0 0 var(--space-xs) 0;
  }

  .site-footer__copyright-subtext {
    color: var(--text-muted);
    font-size: 0.75rem;
    margin: 0;
  }

  .site-footer__additional-links {
    display: flex;
    gap: var(--space-lg);
  }

  .site-footer__additional-link {
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.75rem;
    transition: color var(--transition-fast);
  }

  .site-footer__additional-link:hover {
    color: var(--color-primary);
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .site-footer__main {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-xl);
    }

    .site-footer__brand {
      grid-column: 1 / -1;
    }
  }

  @media (max-width: 768px) {
    .site-footer__container {
      padding: 0 var(--space-md);
    }

    .site-footer__main {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
      padding: var(--space-2xl) 0 var(--space-xl);
    }

    .site-footer__bottom-content {
      flex-direction: column;
      text-align: center;
      gap: var(--space-md);
    }

    .site-footer__copyright {
      text-align: center;
    }

    .site-footer__additional-links {
      justify-content: center;
      flex-wrap: wrap;
      gap: var(--space-md);
    }
  }

  @media (max-width: 480px) {
    .site-footer__main {
      gap: var(--space-lg);
    }

    .site-footer__social-list {
      justify-content: center;
    }

    .site-footer__description {
      text-align: center;
    }
  }

  /* Dark theme adjustments */
  [data-theme="dark"] .site-footer {
    background: linear-gradient(to bottom, var(--bg-primary), var(--bg-secondary));
  }

  [data-theme="dark"] .site-footer__social-link {
    background: var(--bg-secondary);
  }

  [data-theme="dark"] .site-footer__social-link:hover {
    background: var(--bg-card);
  }

  /* Animation for footer appearance */
  @keyframes footer-fade-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .site-footer {
    animation: footer-fade-up 0.6s ease-out;
  }

  /* Focus styles for accessibility */
  .site-footer__nav-link:focus-visible,
  .site-footer__social-link:focus-visible,
  .site-footer__additional-link:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
    border-radius: var(--radius-sm);
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    .site-footer__nav-link,
    .site-footer__social-link,
    .site-footer__additional-link {
      border: 1px solid transparent;
    }

    .site-footer__nav-link:hover,
    .site-footer__social-link:hover,
    .site-footer__additional-link:hover {
      border-color: var(--color-primary);
    }
  }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    .site-footer,
    .site-footer__nav-link,
    .site-footer__social-link,
    .site-footer__additional-link {
      animation: none;
      transition: none;
    }
  }
</style>